# Gradle
/.gradle/
/build/

# Legacy Maven (kept so old local output is ignored)
/target/
dependency-reduced-pom.xml

# IDE
/.idea/
/bin/
/.classpath
/.project
.settings
.factorypath
SimpleClans.iml

# Build artifacts — but keep the Gradle wrapper jar
*.jar
!gradle/wrapper/gradle-wrapper.jar
